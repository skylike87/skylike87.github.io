name: Create Daily Blog Issue
on:
  schedule:
    - cron: '0 0 * * *' # 매일 자정 (UTC), 한국시간 오전 9시
  workflow_dispatch: # 수동 실행 허용

jobs:
  create_issue:
    runs-on: ubuntu-latest
    permissions:
      issues: write
    steps:
      - name: Get current date
        id: date
        run: echo "date=$(date +'%Y-%m-%d')" >> $GITHUB_OUTPUT

      - name: Create Issue
        run: |
          gh issue create \
            --title "[Daily-Signal] ${{ steps.date.outputs.date }}: 오늘의 기록" \
            --body "## 📝 기록할 내용을 코멘트로 남겨주세요.
            에이전트가 이 코멘트를 읽어 블로그 포스팅으로 가공합니다.
            
            - **핵심 키워드**:
            - **참고 링크**:
            - **코드 조각**:" \
            --label "to-blog"
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
